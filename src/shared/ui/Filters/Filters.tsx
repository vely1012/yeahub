import './Filters.css'

function Filters() {
    // make api hooks to fetch all filters from back. apply them to get all filters

    return(
        <div className="filters">
            <div className="filters__section"></div>
        </div>
    )
}

export default Filters